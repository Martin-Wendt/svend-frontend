import { CapacitorConfig } from '@capacitor/cli';

//Configuration file for Capacitor
const config: CapacitorConfig = {
  appId: 'local.mm.sitrep',
  appName: 'Sitrep',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
